// dado que eu recebo dois números de entrada
// quero somar estes números

function soma(a, b) {
  return a + b
}

soma(3, 5)

// quero subtrair estes números

function subtrai(a, b) {
  return a - b
}

subtrai(4, 2) // números não passo aspas

function subtrai(a, b) {
  return "Seu resultado é " + (a - b) //  concatenando
  // return `seu resultado é ${(a - b)}` -> interpolando
}

subtrai(4, 2) // números não passo aspas

// quero multiplicar estes números

function subtrai(a, b) {
  return "Seu resultado é " + a * b
}

subtrai(4, 2)

// quatro operadores
